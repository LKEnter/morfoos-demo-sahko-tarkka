# Public Routing & Multi-Tenancy Scoping
NEXT_PUBLIC_SITE_URL="https://sahko-tarkka-demo.vercel.app"
NEXT_PUBLIC_SITE_ID="site_playground"

# Demo / marketing deploy (optional): set to 1 for public showcase sites without admin.
# Build succeeds without MORFOOS_AUTH_SECRET; /admin and CMS API routes return 404 at runtime.
# MORFOOS_DEMO=1

# Security (Cookie Signing Key — not required when MORFOOS_DEMO=1)
MORFOOS_AUTH_SECRET="a_very_long_random_string_that_is_totally_secret"

# The Initial Seeding Credentials (Only used when running npx prisma db seed)
MORFOOS_ADMIN_EMAIL="admin@clientname.fi"
MORFOOS_ADMIN_PASSWORD="super-secure-temporary-password"

# Infrastructure Connection
DATABASE_URL="mysql://morfoos:morfoos_pass@127.0.0.1:3307/morfoos_playground"
